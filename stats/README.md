# Control Variates Evaluation - Stats

Change directory to `stats/`. Then run
```bash
bash scripts/finetune_stats_chatbot_para.sh
```
as well as other scripts in `scripts/`. If not, change the `id` loop range accordingly.
